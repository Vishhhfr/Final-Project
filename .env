# Frontend
VITE_API_URL=http://localhost:5000

# Backend (these are for reference only, the backend uses its own .env file)
MONGODB_URI=mongodb://127.0.0.1:27017/fuelmate
PORT=5000
JWT_SECRET=your_jwt_secret_key_here 